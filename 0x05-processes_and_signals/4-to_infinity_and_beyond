#!/usr/bin/env bash
#-----------------------
# displays this running script PID
#-----------------------
# ps aux | grep $0 | grep -v grep
while :
do
    echo "To infinity and beyond"
    sleep 2
done
